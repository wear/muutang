<% semantic_form_for @form,:url => event_form_path(@event),:html => {:id => 'form'} do |form| -%>     
   <%=render :partial => 'form',:locals => {:form => form}%>
	<br />
	<%= form.buttons %>   
<% end -%>
